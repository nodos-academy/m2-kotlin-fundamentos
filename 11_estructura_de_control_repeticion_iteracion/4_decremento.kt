// Algoritmo que utiliza un ciclo mientras para contar en reversa desde 5 hasta 1
// En cada iteración, imprime el valor actual y lo decrementa en uno

fun main() {

    var variable = 5.0

    while (variable > 0) {
        println("La variable contiene el dato: $variable")
        variable-- //decremento:  variable = variable - 1
    }
}
